---
export interface Props {
  title: any;
  excerpt: any;
  slug: any;
}
const { title, excerpt, slug } = Astro.props as Props;
---

<div class="blog">
  <h3>{title}</h3>
  <p>{excerpt}</p>
  <a class="btn" href={`/${slug}`}> Read More</a>
</div>
